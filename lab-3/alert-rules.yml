groups:
  - name: My alert rules
    rules:
      - alert: PrometheusTargetMissing
        expr: up == 0
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: 'Prometheus scrape target is missing. instance: {{ $labels.instance }} '
